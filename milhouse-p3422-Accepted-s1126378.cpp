#include<bits/stdc++.h>
using namespace std;

int main(){
	ios_base::sync_with_stdio(false); cin.tie(0);
	int n,c,p,x;
	cin>>n;
	while(n--){
		cin>>c>>p;
		for(int i=0; i<p;i++){
			cin>>x;
			cin>>x;
		}
		cout<<(c-1)<<endl;
	}
	return 0;
}